# GitHub OAuth App
GITHUB_ID=your_github_oauth_client_id
GITHUB_SECRET=your_github_oauth_client_secret

# NextAuth / Auth.js v5
# AUTH_URL is required for production - set to your full app URL (e.g., https://your-app.vercel.app)
# For local development, this defaults to http://localhost:3000
AUTH_URL=http://localhost:3000
# Alternative: NEXTAUTH_URL (for backwards compatibility)
# NEXTAUTH_URL=http://localhost:3000

# Auth secret - required in production
# To generate, run: openssl rand -base64 32
AUTH_SECRET=generate_a_random_string_here
# Alternative: NEXTAUTH_SECRET (for backwards compatibility)
# NEXTAUTH_SECRET=generate_a_random_string_here
